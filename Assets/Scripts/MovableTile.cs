using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovableTile : MonoBehaviour
{
    // List<Vector3> neighbours = new List<Vector3>();
    // List<Vector3> neighboursCoordinates = new List<Vector3>();
    // GameObject selectedTank;
    // static float zOffset = 1.73f;

    // public bool HighlightNeighbours(){
    //     neighbours.Add(new Vector3(-1, 0, 1));
    //     neighbours.Add(new Vector3(-2, 0, 0));
    //     neighbours.Add(new Vector3(-1, 0, -1));
    //     neighbours.Add(new Vector3(1, 0, 1));
    //     neighbours.Add(new Vector3(2, 0, 0));
    //     neighbours.Add(new Vector3(1, 0, -1));

    //     foreach(Vector3 neighbour in neighbours){
    //         selectedTank.transform.position += neighbour;
    //     }
    // }

    // public bool CheckMove(){
    //     neighbours.Add(new Vector3(-1, 0, 1));
    //     neighbours.Add(new Vector3(-2, 0, 0));
    //     neighbours.Add(new Vector3(-1, 0, -1));
    //     neighbours.Add(new Vector3(1, 0, 1));
    //     neighbours.Add(new Vector3(2, 0, 0));
    //     neighbours.Add(new Vector3(1, 0, -1));

    //     foreach(Vector3 neighbour in neighbours){
    //         selectedTank.transform.position += neighbour;
    //     }

    //     return 
    // }
}
